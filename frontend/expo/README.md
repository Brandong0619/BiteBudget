# Expo app (Person C)

Placeholder for the Expo mobile client.

- Call Person B's FastAPI (`POST /api/recommendations`) with budget, goal, and device lat/lng.
- Mapbox (free tier) can pin `lat` / `lng` / `address` / `name` from each recommendation option.
- Do not put API keys in this repo — use local `.env` (gitignored).
